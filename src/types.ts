export interface detail {
    name?: string;
}

export interface post {
    title?: string;
    des?: string;
    price?: string;
    city?: string;
}

export interface farmer{
	name ?: string
	city ?: string
	id ?: string
}